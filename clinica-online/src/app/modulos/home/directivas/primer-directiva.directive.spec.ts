import { PrimerDirectivaDirective } from './primer-directiva.directive';

describe('PrimerDirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimerDirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
