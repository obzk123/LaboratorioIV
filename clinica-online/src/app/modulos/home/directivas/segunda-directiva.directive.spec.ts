import { SegundaDirectivaDirective } from './segunda-directiva.directive';

describe('SegundaDirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new SegundaDirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
