import { PipeEspecialidadPipe } from './pipe-especialidad.pipe';

describe('PipeEspecialidadPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeEspecialidadPipe();
    expect(pipe).toBeTruthy();
  });
});
